@model IEnumerable<Subs.Data.ActiveSubscription>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DisplayActiveSubscriptions</title>
</head>
<body>
    @*<p>
            @Html.ActionLink("Create New", "Create")
        </p>*@

    @ViewBag.Message

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubscriptionId)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.StartIssueDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastIssueDescription)
            </th>
            <th></th>
        </tr>

        <tr></tr>

        @foreach (var item in Model)
        {
            <tr>
                @*  *@
            <td>
                @Html.DisplayFor(modelItem => item.SubscriptionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartIssueDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastIssueDescription)
            </td>

        </tr>
    }

    </table>
</body>
</html>
